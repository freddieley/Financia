/**
 * pending transaction → settled
 * source quantity decreases correctly
 * destination quantity increases correctly
 * insufficient quantity at settlement → failed
 * already-settled transaction → rejected
*/