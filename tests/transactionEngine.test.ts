/**
 * valid intent → pending transaction
 * missing asset → rejected
 * missing position → rejected
 * insufficient quantity → rejected
 * missing permission → rejected
 * policy violation → rejected
 * approval-required intent → doesn't silently become executable
*/